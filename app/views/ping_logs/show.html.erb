<div class="page-header">
  <h1>
    詳細ログ
    <small>pingの詳細ログを表示する画面</small>
  </h1>
</div>

<dl class="dl-horizontal">
  <dt><strong>監視日時:</strong></dt>
  <dd><%= @ping_log.date %></dd>
  <dt><strong>対象サーバ:</strong></dt>
  <dd><%= link_to @ping_log.server.address, @ping_log.server %></dd>
  <dt><strong>状態:</strong></dt>

  <dd>
    <% case @ping_log.status %>
      <% when 'Success' %>
        <span class="label label-success">Success</span>
      <% when 'Failed' %>
        <span class="label label-important">Failed</span>
    <% end %>
  </dd>
  <dt><strong>応答時間(最小/平均/最大/標準偏差)[ms]:</strong></dt>
  <dd><%= "#{@ping_log.min}/#{@ping_log.avg}/#{@ping_log.max}/#{@ping_log.stddev}" %></dd>
  <dt><strong>パケットロス[%]:</strong></dt>
  <dd><%= @ping_log.packet_loss %></dd>
  <dt><strong>ping実行ログ:</strong></dt>
  <dd>
    <pre><%= @ping_log.ping_detail %></pre>
  </dd>
</dl>

<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              server_ping_logs_path(@server), :class => 'btn'  %>
</div>
