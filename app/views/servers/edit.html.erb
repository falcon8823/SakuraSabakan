<div class="page-header">
  <h1>
  	サーバ情報の編集
  	<small>サーバの情報および監視設定をする画面</small>
  </h1>
</div>
<%= render :partial => 'form' %>

  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              server_path(@server),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
